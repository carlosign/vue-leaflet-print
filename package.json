{
  "name": "vue-leaflet-print-monorepo",
  "private": true,
  "packageManager": "pnpm@10.14.0",
  "workspaces": [
    "packages/*",
    "playground"
  ],
  "scripts": {
    "build": "pnpm -r --filter @carloschar/vue-leaflet-print build",
    "dev": "pnpm -C playground dev",
    "test": "pnpm -r test --if-present",
    "release": "pnpm -C packages/core release",
    "lint": "echo \"(opcional) agrega eslint aquí\""
  },
  "license": "MIT"
}
